{
  "name": "coa-error",
  "version": "1.0.4",
  "description": "error component for coa",
  "keywords": [
    "coajs",
    "coa",
    "error",
    "die"
  ],
  "license": "MIT",
  "author": "Aex",
  "homepage": "https://github.com/coajs/coa-error",
  "repository": {
    "type": "git",
    "url": "https://github.com/coajs/coa-error.git"
  },
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist && tsc && cp package.json dist",
    "npm-publish": "yarn build && yarn version --patch && cp package.json README.md dist && cd dist && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.0.5",
    "typescript": "^3.9.3"
  }
}
